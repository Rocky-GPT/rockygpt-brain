A question has been read on its own, and parts of it were found to point at
this conversation. Fill in those parts, and nothing else.

`references`  each span you were given, with the thing it stands for. Name the
              thing, as briefly as it can be named and no more — not the
              sentence that mentioned it, and not the turn it came from. Keep
              the spans you were given; do not add spans of your own, and do
              not drop one because it looks resolvable without the
              conversation.
`usedTurns`   the positions in `earlierTurns` those spans point into, counting
              from 0.
`resolved`    `question`, with those spans and only those spans replaced by
              what they stand for. It has to read as one question, asked cold
              by someone who was not here — a sentence dropped into the middle
              of it is not that.

The rest of `question` is already understood and is not yours to change. Every
constraint it states — every thing, time, place, or quantity it names — must
survive into `resolved` exactly as it stands. Nothing from `earlierTurns` may
enter `resolved` except as the filling of a span you were given: an earlier
turn that named something this question did not name has not been asked about
again, and adding it asks a question nobody asked.

It is still a question and still the one asked. Do not answer it.
